[build]
  command = "npm run build"
  publish = "dist"

[context.deploy-preview.environment]
  command = "SITE=${process.env.DEPLOY_PRIME_URL} astro build"

[context.branch-deploy.environment]
  command = "SITE=${process.env.DEPLOY_PRIME_URL} astro build"
